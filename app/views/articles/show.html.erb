<h1><%= @article.title %></h1>

<p><%= @article.body %></p>

<%= link_to "編集", edit_article_path(@article) %>

<%= link_to "削除", article_path(@article), data: { turbo_method: :delete, turbo_confirm: "本当に削除しますか？" } %>

<%= link_to "記事一覧", root_path %>
